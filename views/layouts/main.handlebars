<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NeoTech News Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300&family=Roboto+Mono:wght@300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>

<body class="" >
    <header class="p-3 bg-dark text-white">
        <div class="container-fluid">
            <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                <h3 class="text-start" >
                   <span id="banner" class="badge bg-warning text-dark ">NeoTech News Blog</span>
                </h3>
                <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 mb-md-0 justify-content-center">
                    <li>                     
                        <a href="/" class="nav-link px-2 text-secondary">Home</a>
                    </li>
                    <li>
                        <a href="/dashboard" class="nav-link px-2 text-white">Dashboard</a>
                    </li>
                </ul>
                <div class="text-end">
                    {{#if loggedIn}}
                    <a href="/logout">
                        <button type="button" id="logout" class="btn btn-outline-light me-2">logout</button>
                    </a>
                    {{else}}
                    <a href="/login">                    
                        <button type="button" class="btn btn-outline-light me-2" id="login" class="btn-no-style">login</button>
                    </a>
                    <a href="/signup">
                        <button type="button" class="btn btn-warning" >Sign-up</button>
                    </a>
                    {{/if}}
                </div>
            </div>
        </div>
    </header>
    <main class="container">
        {{{ body }}}
    </main>
    
    <div class="cotainer fixed-bottom">
        <footer class="footer bg-dark"></footer>
        <div class="col-md-12 d-flex justify-content-center align-items-center bg-dark">
            <span class="text-light">© 2022 MCMDev, inc.</span>
        </div>
        </footer>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>